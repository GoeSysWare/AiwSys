package(default_visibility = ["//visibility:public"])


cc_library(
    name = "poco",
    srcs = glob([
        "build/lib/*.so.*",
        "build/lib/*.so",
    ]),
    includes = [
        "Foundation/include/"
    ],
)
