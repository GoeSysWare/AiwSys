package(default_visibility = ["//visibility:public"])


cc_library(
    name = "poco",
    srcs = glob([
        "lib/*.so.*",
        "lib/*.so",
    ]),
    includes = [
        "include/"
    ],
)
