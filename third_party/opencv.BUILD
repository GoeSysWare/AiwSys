licenses(["notice"])

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "opencv",
    includes = [
        "include",
    ],
    srcs = glob([
        "lib/*.so.*",
        "lib/*.so",
    ]),
    hdrs = glob([
        "include/*.h",
    ]),

)
